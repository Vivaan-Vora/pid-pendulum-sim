            "error": float(error),
            "p_term": float(p_term),
            "i_term": float(i_term),
            "d_term": float(d_term),
            "output": float(output),
        }
        return output
